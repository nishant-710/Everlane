<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Sizes</title>
</head>

<body style="margin: 0; font-family: Arial, sans-serif; background-color: #f4f6f8">
    <%- include('partials/sidebar') %>

        <div style="margin-left: 220px">
            <%- include('partials/header') %>

                <div
                    style="max-width: 1000px; margin: 40px auto; background: white; padding: 30px; border-radius: 10px; box-shadow: 0 0 12px rgba(0, 0, 0, 0.1);">
                    <div
                        style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px">
                        <h2 style="color: #333">All Sizes</h2>
                        <a href="/admin/sizeAdd"
                            style="padding: 8px 16px; background-color: #28a745; color: white; text-decoration: none; border-radius: 4px; font-weight: bold;">+
                            Add Size</a>
                    </div>

                    <table style="width: 100%; border-collapse: collapse; text-align: center;">
                        <tr style="background-color: #f0f0f0">
                            <th style="padding: 12px; border: 1px solid #ddd">Name</th>
                            <th style="padding: 12px; border: 1px solid #ddd">Symbol</th>
                            <th style="padding: 12px; border: 1px solid #ddd">Actions</th>
                        </tr>

                        <% if (size.length> 0) { size.forEach(size => { %>
                            <tr>
                                <td style="padding: 10px; border: 1px solid #ddd">
                                    <%= size.name %>
                                </td>
                                <td style="padding: 10px; border: 1px solid #ddd">
                                    <%= size.symbol %>
                                </td>
                                <td style="padding: 10px; border: 1px solid #ddd">
                                    <div style="display: flex; justify-content: center; gap: 8px">
                                        <a href="/admin/size/<%= size._id %>/edit"
                                            style="padding: 6px 10px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; text-decoration: none;">Edit</a>
                                        <form method="POST" action="/admin/size/<%= size._id %>/delete">
                                            <button
                                                onclick="return confirm('Are you sure you want to delete this size?');"
                                                style="padding: 6px 10px; background-color: #dc3545; color: white; border: none; border-radius: 4px; cursor: pointer;">Delete</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                            <% }) } %>
                    </table>
                </div>
        </div>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Slider Management</title>
</head>

<body style="margin: 0; font-family: Arial, sans-serif; background-color: #f4f6f8">
  <%- include('partials/sidebar') %>

    <div style="margin-left: 220px">
      <%- include('partials/header') %>

        <div style="padding: 30px">
          <div style="display: flex; gap: 10px; margin-bottom: 20px">
            <a href="/admin/sliderAdd">
              <button style="
                padding: 10px 20px;
                background-color: #27ae60;
                color: white;
                border: none;
                border-radius: 4px;
                cursor: pointer;
              ">
                Add Slider
              </button>
            </a>
          </div>
          <h2 style="color: #2c3e50; text-align: center; margin-bottom: 30px">
            Sliders
          </h2>

          <div style="
            max-width: 900px;
            margin: 0 auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.08);
          ">
            <% if (slider.length> 0) {%>
              <table style="width: 100%; border-collapse: collapse">
                <thead>
                  <tr style="background-color: #2c3e50; color: white">
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Mobile Image</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Web Image</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Category</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Product</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Title</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Description</th>
                    <th style="padding: 10px; border: 1px solid #ccc; text-align: center">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% slider.forEach(slid=> { %>
                    <tr>
                      <td style="padding: 10px; border: 1px solid #ddd; display: flex; gap: 8px; flex-wrap: wrap">
                        <% if (slid.imageMobile && slid.imageMobile.length> 0) { %>
                          <% slid.imageMobile.forEach(img=> { %>
                            <img src="/uploads/<%= img %>" alt="Slider" style="width: 100px; border-radius: 6px;" />
                            <% }) %>
                              <% } else { %>
                                <span>No Mobile Image</span>
                                <% } %>
                      </td>

                      <td style="padding: 10px; border: 1px solid #ddd">
                        <% if (slid.imageWeb && slid.imageWeb.length> 0) { %>
                          <% slid.imageWeb.forEach(img=> { %>
                            <img src="/uploads/<%= img %>" alt="Slider"
                              style="width: 100px; border-radius: 6px; margin: 5px;" />
                            <% }) %>
                              <% } else { %>
                                <span>No Image</span>
                                <% } %>
                      </td>

                      <td style="padding: 10px; border: 1px solid #ddd; color: #34495e">
                        <%=slid.category.name%>
                      </td>
                      <td style="padding: 10px; border: 1px solid #ddd; color: #34495e">
                        <%=slid.product.name%>
                      </td>
                      <td style="padding: 10px; border: 1px solid #ddd; color: #34495e">
                        <%=slid.title%>
                      </td>
                      <td style="padding: 10px; border: 1px solid #ddd; color: #34495e">
                        <%=slid.description%>
                      </td>
                      <td style="padding: 10px; border: 1px solid #ddd; text-align: center">
                        <a href="/admin/slider/<%=slid._id%>/edit" style="
                      padding: 6px 12px;
                      background-color: #f39c12;
                      color: white;
                      border: none;
                      border-radius: 4px;
                      text-decoration: none;
                      display: inline-block;
                      margin-right: 5px;
                    ">
                          Edit
                        </a>

                        <form method="POST" action="/admin/slider/<%=slid._id%>/delete" style="display: inline-block">
                          <button type="submit" onclick="return confirm('Are you sure you want to delete this slider?')"
                            style="
                        padding: 6px 12px;
                        background-color: #c0392b;
                        color: white;
                        border: none;
                        border-radius: 4px;
                        cursor: pointer;
                      ">
                            Delete
                          </button>
                        </form>
                      </td>
                    </tr>
                    <%})%>
                </tbody>
              </table>
              <% } else { %>
                <p style="text-align: center; color: #7f8c8d">No sliders found.</p>
                <% } %>
          </div>
        </div>
    </div>
</body>

</html>